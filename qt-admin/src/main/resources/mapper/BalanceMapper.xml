<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quant.admin.dao.BalanceMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.quant.admin.entity.Balance">
        <id column="id" property="id" />
        <result column="account_id" property="accountId" />
        <result column="type" property="type" />
        <result column="currency" property="currency" />
        <result column="trade_balance" property="tradeBalance" />
        <result column="frozen_balance" property="frozenBalance" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, account_id AS accountId, type, currency, trade_balance AS tradeBalance, frozen_balance AS frozenBalance
    </sql>

</mapper>
